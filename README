TODO: Document what changed, how this works

Base Buildroot Version : 2022.08

# apply config
make BR2_EXTERNAL=ext sureshot_defconfig

# build it
make all

# to edit config
make BR2_EXTERNAL=ext menuconfig

# to update saved config
make savedefconfig BR2_DEFCONFIG=ext/config/sureshot_defconfig



